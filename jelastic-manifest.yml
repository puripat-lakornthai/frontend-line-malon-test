type: install
version: 1.0
id: react-frontend
name: React Frontend
onInstall:
  - cmd [npm ci]
  - cmd [npm run build]
  - cmd [rm -rf /var/www/webroot/ROOT/*]
  - cmd [cp -r build/* /var/www/webroot/ROOT/]
